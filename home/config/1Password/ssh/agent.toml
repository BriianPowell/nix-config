# https://developer.1password.com/docs/ssh/agent/config
#
# Order matters: keys are offered top-to-bottom.

[[ssh-keys]]
item = "Personal"
vault = "Tech Stack"

[[ssh-keys]]
item = "NixOS Admin"
vault = "Tech Stack"

# After sheol/abaddon rebuild, put NixOS Admin first for server login.
# Server GitHub deploy keys live on the hosts (agenix), not needed on Mac.
# [[ssh-keys]]
# item = "Sheol"
# vault = "Tech Stack"
#
# [[ssh-keys]]
# item = "Abaddon"
# vault = "Tech Stack"
